# conda create env -f cross_platform_env.yml
# conda env update -f cross_flatform_env.yml
name: solvers
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - glpk
  - ipopt
